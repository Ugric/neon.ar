<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <title>{{title}}</title>
    </head>
    <body>
        <h1>Home</h1>
        <p>Hi, {{username}}</p>
        {%- for char in 10|range-%}
        <h1>{{char}} {{loop.index}}</h1>
        <h1>Hello, {%if username=="sus"-%}
            <h1>{{username}}</h1>
        {%-elif username=="Ugric"-%}
            Master
        {%-elseif username=="Oli"-%}
            sussy baka
            
        {%-else-%}
        Mortal
        {%-endif%}</h1>
        {%- endfor %}
    </body>
</html>