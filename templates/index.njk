<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <title>{{title}}</title>
    </head>
    <body>
        <h1>Home</h1>
        <p>Hi,
            {{username|length}}</p>
        {%if title|length -%}
            <h1>{{username}}</h1>
            {%- endif%}
        </body>
    </html>